export default interface ICreateProductDTO {
  sku?: string;
  description: string;
  balance: number;
  category_code: string,
  color_code: string,
  model_code: string,
  size_code: string,
// eslint-disable-next-line semi
}
